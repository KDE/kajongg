#!/bin/sh

# This script runs kajongg in automode for testing

if test "$#" -ne 1
then
	echo Usage: $0 seed
 	exit 2
fi

seed=$1
while true
do
	echo seed:$seed
	kajongg --automode --playopen --seed=$seed
	seed=`expr $seed + 1`
	echo
	echo
done
